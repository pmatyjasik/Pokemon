pre-commit:
    parallel: true
    commands:
        lint:
            run: yarn lint
        test:
            run: yarn test
        typecheck:
            run: yarn typecheck
commit-msg:
    parallel: true
    commands:
        commitlint:
            run: npx commitlint --edit
